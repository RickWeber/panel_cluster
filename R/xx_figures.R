##
# To illustrate the cluster alignment, generate a 2D plot of a base year including cluster centroids,
# then the year being adjusted (and it's centers) before and after adjustment, 
# with vectors showing the distance minimizing realignment of labels.